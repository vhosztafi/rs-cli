#!/bin/bash
# Pre-commit hook (placeholder)
# Commit message validation is handled by the commit-msg hook

# This hook can be used for other pre-commit checks if needed
# For now, it's a no-op that always succeeds
exit 0
